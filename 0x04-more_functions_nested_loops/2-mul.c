#include"main.h"

/**
  mul - function to multiply two numbers
  *
  *@a: first number
  *@b: second number
  *Return: always 0
  */

int mul(int a, int b)
{
	int result = a * b;

	putchar(result);
	return (0);
}

